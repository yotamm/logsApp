import { SanitizeAnsiDirective } from './sanitize-ansi.directive';

describe('SanitizeAnsiDirective', () => {
  it('should create an instance', () => {
    const directive = new SanitizeAnsiDirective();
    expect(directive).toBeTruthy();
  });
});
